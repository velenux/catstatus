%html{ :lang => 'en' }
  %head
    %title 
      Cat Status 
      = @version
    %link{ :rel => 'stylesheet', :href => '/style.css' }

  %body

    #content

      %h1#title In questo momento, il vostro gatto...
      %ul
        - @statuses.sort.each do |id, name|
          %li
            %a{:href => "/status/add/#{id}"}= name

